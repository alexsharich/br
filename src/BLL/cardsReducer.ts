import React from "react";


/* export const cardsReducer = (state: any, action: any) => {
    switch (action.type) {
        case '':

            return state;

        default:
            return state
    }
} */