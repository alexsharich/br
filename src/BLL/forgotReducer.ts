import React from "react";


export const forgotReducer = (state: any, action: any) => {
    switch (action.type) {
        case 'FORGOT':
            return state;
        default:
            return state
    }
}